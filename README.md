# microservice-math-exam
This repository will show you the implementation of AREP second exam, developing a microservice system prototype to compute math functions deployed in two instances and one proxy instance using round-robin as fallback. Stack is HTML5, JS, Maven. 
